Sub VBAChallenge():

    
    'label the columns on the sheet

    Range("I1:L1").Value = Array("Ticker Symbol", "Yearly Change", "Percent Change", "Total Stock Volume")
    
    'add Tickers
    Dim LastRow As Long
    LastRow = Cells(Rows.Count, 1).End(xlUp).Row
    MsgBox "Last Row: " & LastRow
    
    Dim Column As Integer
    Dim Summary_Table_Row As Integer
    Dim Start_Price As Long
    Dim End_Price As Long
    Dim Change_Price As Long
    
    Summary_Table_Row = 2
    Column = 1
    Start_Price = Cells(2, 3).Value
    
        For i = 2 To LastRow
        If Cells(i + 1, Column).Value <> Cells(i, Column).Value Then
        
            'add code to write Ticker in column I
            Range("I" & Summary_Table_Row).Value = Cells(i, Column).Value
            
        Next i
        
        End If
        
        For i = 2 To LastRow
            If Cells(i + 1, Column).Value <> Cells(i, Column).Value Then
            
            'find the yearly difference from opening price of a given year to closing price of a given year
            End_Price = Cells(i, 3).Value
            Change_Price = End_Price - Start_Price
            
            'add code to write Ticker in column J
            Range("J" & Summary_Table_Row).Value = Change_Pric
            
            'Shift recording line down one
            Summary_Table_Row = Summary_Table_Row + 1
            
            Next i
        End If

    'add percent change
    
    'add total stock volume
    
    'add conditional formatting to the array (seeVBA example "grade book")
    
    'do it to all the sheets
    
    'Challenge: add a new array
        'Greatest % Increase
        'Greatest % Decrease
        'Greatest Total Volume
    'do it to all sheets
    
End Sub
